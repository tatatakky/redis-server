#テスト用にdbに値をsetするsetterを用意。
def setter(r, k):
    pass
    #kの値をkeyとし、keyとvalueセットする処理。
    #for i in range(10):
    #    name = "name_data_" + str(i)
    #    r.set(name, str(i) + "kun")
    #    r.rpush(name, i)
    #    r.rpush(name, i*100)

